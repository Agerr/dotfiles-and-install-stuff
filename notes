# Uncomment to enable multilib repositories
> sudo nano /etc/pacman.conf
#[multilib]
#Include = /etc/pacman.d/mirrorlist



### Installing ###

# Install display server, amd, audio drivers
> sudo pacman -S xorg mesa lib32-mesa xf86-video-amdgpu vulkan-radeon lib32-vulkan-radeon libva-mesa-driver lib32-libva-mesa-driver alsa-utils pulseaudio pulseaudio-alsa

# Install pacman packages
> sudo pacman -S lightdm lightdm-slick-greeter i3 alacritty xdg-user-dirs xss-lock polkit-gnome gnome-keyring feh picom firefox thunar discord gparted deepin-calculator btop tldr tree flameshot gpick dunst ttf-jetbrains-mono ttf-dejavu ttf-droid noto-fonts-emoji gvfs ntfs-3g

# Install yay packages
> yay -S systemd-numlockontty numlockx autotiling nerd-fonts-jetbrains-mono visual-studio-code-bin icu69 gitkraken spotify timeshift betterdiscord-installer



### Enabling / creating stuff ###

# Enable numlock on tty
> sudo systemctl enable numLockOnTty.service

# Enable display manager
> sudo systemctl enable lightdm.service

# Create common User's directories
> xdg-user-dirs-update



### Linking ###

# Link i3
> ln -sf ~/Source/git/dotfiles/.config/i3/config ~/.config/i3/config

# Link i3bar
> mkdir ~/.config/i3status
> ln -sf ~/Source/git/dotfiles/.config/i3status/config ~/.config/i3status/config

# Link desktop background
> ln -s ~/Source/git/dotfiles/Wallpapers/dracula_arch.png ~/Pictures/background.png

# Link display manager background
> sudo mkdir -p /usr/share/backgrounds
> sudo ln -s ~/Source/git/dotfiles/Wallpapers/dracula_base.png /usr/share/backgrounds/background.png

# Link compositor
> sudo ln -sf ~/Source/git/dotfiles/picom.conf /etc/xdg/picom.conf

# Link display manager
> sudo ln -sf ~/Source/git/dotfiles/lightdm.conf /etc/lightdm/lightdm.conf

# Link greeter
> sudo ln -sf ~/Source/git/dotfiles/slick-greeter.conf /etc/lightdm/slick-greeter.conf

# Link alacritty
> ln -sf ~/Source/git/dotfiles/.config/alacritty ~/.config/alacritty

# Link dunst
> ln -sf ~/Source/git/dotfiles/.config/dunst ~/.config/dunst

# Link BetterDiscord theme
> mkdir -p ~/.config/BetterDiscord/themes
> ln -sf ~/Source/git/dotfiles/.config/BetterDiscord/themes/dracula.theme.css ~/.config/BetterDiscord/themes/dracula.theme.css



### Manual installation ###

# BetterDiscord
> betterdiscord-installer